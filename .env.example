# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                           RIVEN FRONTEND                                  ║
# ║                     Environment Configuration                             ║
# ╚═══════════════════════════════════════════════════════════════════════════╝

# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         REQUIRED SETTINGS                                 │
# └───────────────────────────────────────────────────────────────────────────┘

# ─── Backend Connection ──────────────────────────────────────────────────────
# Riven backend URL (e.g., http://localhost:8080)
BACKEND_URL=

# Riven backend API key
BACKEND_API_KEY=

# ─── Database ────────────────────────────────────────────────────────────────
# Database URL (only SQLite is supported)
DATABASE_URL=/riven/data/riven.db

# ─── Authentication ──────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET=random_secret

# Frontend URL used by auth & CSRF protection
# (the URL you'll use to access the frontend)
ORIGIN=http://localhost:5173


# ┌───────────────────────────────────────────────────────────────────────────┐
# │                         OPTIONAL SETTINGS                                 │
# └───────────────────────────────────────────────────────────────────────────┘

# ─── Logging ─────────────────────────────────────────────────────────────────
# Log levels:
#   0    = Fatal and Error
#   1    = Warnings
#   2    = Normal logs
#   3    = Informational (success, fail, ready, start, ...)  [DEFAULT]
#   4    = Debug
#   5    = Trace
#   -999 = Silent
#   +999 = Verbose
#
# PUBLIC_LOG_LEVEL=3
# PUBLIC_LOG_DATE=true           # Show timestamps (default: true)
# PUBLIC_LOG_COLORS=true         # Colorized output (default: true)
# PUBLIC_LOG_COMPACT=false       # Compact log format (default: false)

# ─── Database Options ────────────────────────────────────────────────────────
# DATABASE_LOGGING=false         # Enable database query logging

# ─── Auth Providers ──────────────────────────────────────────────────────────
# Email/Password
# DISABLE_EMAIL_PASSWORD=false
# ENABLE_EMAIL_PASSWORD_SIGNUP=false

# Plex
# DISABLE_PLEX=false
# ENABLE_PLEX_SIGNUP=false

# Passkey/WebAuthn
# PASSKEY_RP_ID=
# PASSKEY_RP_NAME=

# ─── Generic OAuth2/OIDC ─────────────────────────────────────────────────────
# Add any valid OAuth2/OIDC provider using the format:
#   OAUTH_PROVIDER_{NAME}_{KEY}
#
# {NAME} = uppercase provider name (GOOGLE, GITHUB, AUTHENTIK, etc.)
#
# Available keys:
#   CLIENT_ID         (Required) OAuth client ID
#   CLIENT_SECRET     (Required) OAuth client secret
#   ISSUER            OIDC issuer URL (auto-appends /.well-known/openid-configuration)
#   DISCOVERY_URL     Full OIDC discovery URL (alternative to ISSUER)
#   SCOPES            Comma-separated scopes (e.g., openid,profile,email)
#   PKCE              Enable PKCE: true | false
#   DISABLE_SIGNUP    Disable new user sign-up: true | false
#   REDIRECT_URI      Custom redirect URI (default: ${ORIGIN}/api/auth/oauth2/callback/{provider})
#   AUTHORIZATION_URL Authorization endpoint (not needed with ISSUER/DISCOVERY_URL)
#   TOKEN_URL         Token endpoint (not needed with ISSUER/DISCOVERY_URL)
#   USERINFO_URL      User info endpoint (not needed with ISSUER/DISCOVERY_URL)
#   NAME              Display name for the provider
#   ICON              Icon URL (https://selfh.st/icons/ or https://simpleicons.org)
#
# ── Example: Authentik ───
# OAUTH_PROVIDER_AUTHENTIK_CLIENT_ID=your_client_id
# OAUTH_PROVIDER_AUTHENTIK_CLIENT_SECRET=your_client_secret
# OAUTH_PROVIDER_AUTHENTIK_ISSUER=https://authentik.company.com
# OAUTH_PROVIDER_AUTHENTIK_SCOPES=openid,profile,email
# OAUTH_PROVIDER_AUTHENTIK_PKCE=true
# OAUTH_PROVIDER_AUTHENTIK_NAME=Authentik
# OAUTH_PROVIDER_AUTHENTIK_ICON=https://cdn.jsdelivr.net/gh/selfhst/icons/svg/authentik.svg
#
# ── Example: Google ───
# OAUTH_PROVIDER_GOOGLE_CLIENT_ID=your_client_id
# OAUTH_PROVIDER_GOOGLE_CLIENT_SECRET=your_client_secret
# OAUTH_PROVIDER_GOOGLE_DISCOVERY_URL=https://accounts.google.com/.well-known/openid-configuration
# OAUTH_PROVIDER_GOOGLE_SCOPES=openid,profile,email
# OAUTH_PROVIDER_GOOGLE_NAME=Google
# OAUTH_PROVIDER_GOOGLE_ICON=https://cdn.jsdelivr.net/gh/selfhst/icons/svg/google.svg
#
# ── Example: Keycloak ───
# OAUTH_PROVIDER_KEYCLOAK_CLIENT_ID=your_client_id
# OAUTH_PROVIDER_KEYCLOAK_CLIENT_SECRET=your_client_secret
# OAUTH_PROVIDER_KEYCLOAK_ISSUER=https://keycloak.company.com/realms/myrealm
# OAUTH_PROVIDER_KEYCLOAK_PKCE=true
# OAUTH_PROVIDER_KEYCLOAK_NAME=Keycloak
# OAUTH_PROVIDER_KEYCLOAK_ICON=https://cdn.jsdelivr.net/gh/selfhst/icons/svg/keycloak.svg

# ─── External APIs ───────────────────────────────────────────────────────────
# PUBLIC_TMDB_READ_ACCESS_TOKEN=
# PUBLIC_TRAKT_CLIENT_ID=


# ┌───────────────────────────────────────────────────────────────────────────┐
# │                        DEVELOPMENT SETTINGS                               │
# └───────────────────────────────────────────────────────────────────────────┘

# These are also the defaults, so not strictly necessary to set
# DRIZZLE_SCHEMA_PATH=./src/lib/server/schema
# DRIZZLE_MIGRATIONS_PATH=drizzle